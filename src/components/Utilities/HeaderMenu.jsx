const HeaderMenu = ({ title }) => {
  return (
    <div className="pb-4">
      <h1 className="text-xl text-center text-white font-semibold">{title}</h1>
    </div>
  );
};

export default HeaderMenu;
